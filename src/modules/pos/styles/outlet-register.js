import {Dimensions, StyleSheet} from "react-native";

const deviceHeight = Dimensions.get("window").height;
const deviceWidth  = Dimensions.get("window").width;

const style = {
  container : {},
  textHeader: {}
};

export const outletRegisterStyles = style;
